- python code

```python
def solution(num):
    return "Even" if not num%2 else "Odd"
```

- cpp code

```cpp
#include <string>
#include <vector>

using namespace std;

string solution(int num) 
{
    return (!(num%2)) ? "Even": "Odd";
}
```
